language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-muzzley-generator' ]; then cd .. && eval "mv $currentfolder generator-muzzley-generator" && cd generator-muzzley-generator; fi

